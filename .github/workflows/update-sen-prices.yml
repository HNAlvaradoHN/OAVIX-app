name: Update SEN fuel prices

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * 5'

permissions:
  contents: write

jobs:
  update-sen-prices:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder until SEN data source is wired
        run: |
          echo "SEN automatic updater scaffold is ready."
          echo "No values are generated or replaced until the official source parser is verified."
